this is a file his name office.txt
Git use for devops 

Some important commands
1>Mkdir project (crate a folder which has help to run git )
2>Git init  ( The git init command creates a new Git repository. It can be used to convert an existing, unversioned project to a Git repository or initialize a new, empty repository.

3>Sudo nano vijay.txt
4>Git add .The git add command adds a change in the working directory to the staging area.
5>Git commit -m “vijay.txt”(git commit -a. Commit a snapshot of all changes in the working directory.
6>Git status (The git status command displays the state of the working directory and the staging area.
7>Git log (The git log command is Git's basic tool for exploring a repository's history. It's what you use when you need to find a specific version of a project or figure out what changes will be introduced by merging in a feature branch.
8>Git show (fill commit ID)git-show is a command line utility that is used to view expanded details on Git objects such as blobs, trees, tags, and commits

9>Git remote add origin (fill your repositry url aur github account):- In Git, "origin" is a shorthand name for the remote repository that a project was originally cloned from. More precisely, it is used instead of that original repository's URL - and thereby makes referencing much easier. Note that origin is by no means a "magical" name, but just a standard convention

10>Git push -u origin Master The command `git push origin master` can be used to push any commits made locally on the `master` branch to a remote repository on `origin`
11>Fill user name 
12>Password (you need to fill in password access tocken in your github account
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   token for github
